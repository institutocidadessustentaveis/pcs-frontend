import { HasPerfilPipe } from './has-perfil.pipe';

describe('HasPerfilPipe', () => {
  it('create an instance', () => {
    const pipe = new HasPerfilPipe();
    expect(pipe).toBeTruthy();
  });
});
