import { CorReferenciaPipe } from './cor-referencia.pipe';

describe('CorReferenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new CorReferenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
