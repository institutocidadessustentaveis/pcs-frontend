import { GeradorUrlCidadePipe } from './gerador-url-cidade.pipe';

describe('GeradorUrlCidadePipe', () => {
  it('create an instance', () => {
    const pipe = new GeradorUrlCidadePipe();
    expect(pipe).toBeTruthy();
  });
});
